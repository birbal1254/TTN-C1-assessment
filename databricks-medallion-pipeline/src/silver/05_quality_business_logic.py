"""
Silver layer: Data quality check — business logic.

Placeholder: Apply domain rules (positive quantities, valid order status, amount consistency).
"""

# TODO: quantity > 0
# TODO: price and total_amount >= 0
# TODO: order_date not in future (configurable)
# TODO: valid status values (e.g. completed, pending, cancelled)
# TODO: Log business rule violations
